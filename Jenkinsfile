pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Build success 🚀'
            }
        }

        stage('Test') {
            steps {
                echo 'Running test...'
                sh 'echo test passed ✅'
            }
        }
    }
}
